import 'package:flutter/material.dart';

class BaseLayerWidget extends StatelessWidget {
  const BaseLayerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
    );
  }
}
